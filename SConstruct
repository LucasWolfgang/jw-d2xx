import os

SharedLibrary('jw_d2xx', 'main.cpp', LIBS="ftd2xx", LIBPATH='.',
              CPPPATH = [f"{os.environ['JAVA_HOME']}/include",
                     f"{os.environ['JAVA_HOME']}/include/win32"])